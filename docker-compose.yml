services:
  bot:
    build: ./bot
    restart: unless-stopped
    env_file:
      - stack.env
    volumes:
      - ./data:/app/data
      - ./scripts:/app/scripts
    environment:
      - FETCH_SCRIPT=/app/scripts/new_script.py
      - DATA_DIR=/app/data
